# 更新记录

本文件记录对使用者可见的变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.1.9] — 2026-09-15

### 变更

- **安装包下载和应用内更新都换到了国内可直连的地址。** 此前两者都在 GitHub 上，下载慢、经常连不上；应用查更新走的是同一条路，连不上就一直查不到新版本，停在初次安装的那一版。现在不挂代理也能正常下载和更新

## [0.1.8] — 2026-09-15

### 修复

- **Intel Mac 上后台服务根本起不来。** 这个问题从 0.1.5 起就存在，一直没被发现：0.1.5 为了通过 Apple 公证给所有组件开启了「加固运行时」，而加固运行时禁止一块内存既可写又可执行。后台服务在启动时要用到这个能力（Python 调用系统接口的底层机制依赖它），申请失败后它不会报错，而是无限重试——表现就是服务卡在启动中，界面一直显示「服务异常」，重装、重启、清理进程都没用。Apple Silicon 的 Mac 走的是另一条实现路径，所以本机测试从来没暴露出来。现在后台服务单独声明了这项权限，加固运行时和公证都保持不变
- **升级之后后台服务反复重启。** 覆盖安装新版本时，旧版本的后台服务不会跟着退出，它继续占着手柄、继续写状态文件，新版本的服务又在旁边起了一个。两个服务打架，界面就会显示「服务未运行」，点重启也没用。现在打开 App 会先把残留的旧服务清掉
- **状态文件读不出来时，不再谎报成「服务没在运行」。** 之前这两种情况走同一条分支，结果是明明服务活着，界面却让你去点重启
- 后台服务现在会记录自己的进程号和退出原因。之前它在启动途中被中止的话，日志里只留一行，看不出发生过什么——这次的问题就因此多花了一轮排查

## [0.1.7] — 2026-09-15

### 变更

- **安装包再小一点**：23MB → 21MB。界面里的手柄插图和应用图标此前是按原图打包的，分辨率远高于屏幕上真正用到的尺寸（图标是 1024×1024，而界面里只有 94 点大）。现在按实际显示尺寸打包，视觉上没有差别

## [0.1.6] — 2026-09-14

### 新增

- **应用内自动更新。** 以后有新版本会在应用里提示，点一下就能装好，不用再自己去下载安装包。默认每天检查一次，发现新版本会先问你，**不会自己装**。不想要可以在「关于 → 系统」关掉
- 同一处还有「检查」按钮，随时可以手动查一次

### 变更

- **安装包从 81MB 降到 23MB。** 两处来源：之前包里带着一整套游戏多媒体库（音频、图像、字体解码），而它只被用来在启动时打一行日志，真正读手柄的代码从来没用过它；磁盘映像也改用了压缩率更高的算法

## [0.1.5] — 2026-09-14

### 变更

- **安装包已通过 Apple 公证。** 首次打开不会再被系统拦下，也不用再去「系统设置 → 隐私与安全性」点「仍要打开」—— 双击直接就能开
- 安装包内所有组件都改为 Developer ID 签名（此前内置运行时的组件是临时签名）

## [0.1.4] — 2026-09-14

### 修复

- **修掉了「用着用着就没反应了」。** 后台服务会在运行几小时后意外退出，界面还在、手柄却失灵。根因是读取左右手柄的两个线程同时调用同一个 HID 库，而那个库不允许这样用
- **不会再留下多余的后台服务。** 之前 App 异常退出时，它启动的后台服务会被留在系统里继续跑，和下次打开的那个抢手柄、抢状态文件，表现为状态乱跳或手柄时灵时不灵。现在 App 一走，后台服务跟着退出，并且同时只允许有一个

### 变更

- **手柄空闲自动休眠改为默认开启**：闲置 10 分钟后手柄断开并关机，按任意键唤醒。不想要可以到「关于 → 系统」关掉。已经在用的用户保持原有设置不变

## [0.1.3] — 2026-09-14

### 修复

- **Intel Mac 现在能装了。** v0.1.2 的安装包只带了 Apple Silicon 的版本，在 Intel Mac 上打开会直接被拦下：「这台 Mac 不支持此应用程序」。0.1.3 起是**通用版本**，同一个 DMG 在两种 Mac 上都能装，下载时不用分辨自己是哪一种

## [0.1.2] — 2026-09-14

### 变更

- 引导里把 `fn` 说清楚了：它是**语音输入法的启动快捷键**，不特指某一家工具。JoyHarness 只负责把 `fn` 发出去，要让它有反应，得先在你自己的语音输入法里把启动快捷键设成 `fn`
- 之前的说法是「按 ZR 启动 Typeless」—— 既把一个厂商写成了产品本身，也藏起了责任边界。新用户按下去没反应，会以为是产品坏了，而那恰恰是它在正常工作
- README 改为面向使用者，配上真实界面截图

## [0.1.1] — 2026-09-13

上线前审查的修复。都是「功能是通的，但界面说的和实际做的不是一回事」那一类。

### 修复

- 首次安装后「登录时启动」显示「当前不可用」，并让你把 App 放进「应用程序」文件夹 —— 而它已经在那里；开关本身一直是好的
- 点过「稍后设置」之后，四步引导仍然每次启动都重新弹出。而它的最后一步需要先授权、先连上手柄、再按满四次，没有这三样的人永远关不掉它
- 服务没在运行时保存按键配置：界面五秒没有任何反应，面板不关，等你点了「取消」才弹出一句自相矛盾的错误 —— 说「自动恢复失败」，而配置其实已经正确恢复了，同一句话还重复了两遍。现在提示直接出现在面板里，一句话，并告诉你去哪里重启
- 「长按」在不同按键上其实是两个时长：内置动作 0.25 秒，分档按键 0.35 秒，界面上都叫「长按」。现在全产品一个值，写在配置文件顶部
- 手柄休眠时菜单栏显示「未连接」，而同一时刻界面上写着「已休眠 · 按任意键唤醒」
- 未完成授权时，连接页仍然写着「按键正在发出快捷键」，就在红色「还需要完成系统授权」横幅的下面
- 「关于」页显示的版本号与实际发布的版本对不上（按文档构建出来会标成 0.2.0）
- 窗口相关功能一直在走慢速回退路径：打包时漏装了一个依赖，导致每次查询窗口都要起一个 osascript 子进程
- `--list-controls` 把十六条映射里的十五条打印成 `?`

### 移除

- 「聚焦窗口」不再出现在按键编辑器里。它的长按那一档需要一个浮层，而打包版本里根本没有；第一档也只能在一个写死的 App 的窗口之间循环，界面上没有地方改。已经手工配置过的仍然照常工作
- 默认配置里不再带 `known_apps`：没有任何代码读它，而它把作者自己的应用列表发给了每一个用户

### 其他

- 上线前审查的检查脚本重写为 133 条断言，逐条比对「同一个事实的不同副本是否还一致」，而不是搜索某一行代码是否还在

## [0.1.0] — 2026-09-12

第一个可日常使用的版本。私有仓库，尚未公开发布，也尚未公证。

### 新增

- 支持左右 Joy-Con，可单只或成对使用；相同物理位置的按键功能一致
- 每个按键可配置为单一功能，或按一下与按住分别触发不同快捷键
- 在界面上直接录制快捷键，支持 `⌃fn` 这类只有修饰键的组合
- 首次使用引导：授权、连接、按键测试逐项自动检测，不需要手动确认
- 按住连发的节奏跟随系统键盘设置
- 配置每次保存自动备份，可随时回退
- 可选的手柄空闲自动休眠，避免长时间连接耗电（默认关闭）
- 聚焦输入框：把光标送进当前窗口的输入框，不针对具体应用做适配
- 长按到达时的震动反馈，让你知道什么时候可以松手

### 修复

- 手柄显示已连接但按键没有反应
- 左手柄的按键映射与界面显示不一致
- 左手柄 SL / SR 上下颠倒
- 改过的默认配置无法送达已安装的版本
- 回车发送有明显延迟
- 服务异常退出后界面仍显示一切正常
- 单侧手柄会在某些情况下永久失效
- 长时间使用后界面卡顿

### 已知限制

- 尚未公证，需要从源码构建安装
- 手柄连接期间不会自动进入休眠，长时间挂着比较费电
- 界面目前只有简体中文
